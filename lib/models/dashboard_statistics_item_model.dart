class DashboardStatisticsItemModel {
  final String title, amount, mensCount, womensCount;

  const DashboardStatisticsItemModel({
    required this.title,
    required this.amount,
    required this.mensCount,
    required this.womensCount,
  });
}
